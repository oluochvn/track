{
  "name": "paytrack-root",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm --prefix server run dev\" \"npm --prefix paytrack run dev\"",
    "dev:server": "npm --prefix server run dev",
    "dev:client": "npm --prefix paytrack run dev"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
